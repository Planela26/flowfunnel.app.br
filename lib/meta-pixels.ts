// Shared Meta Pixel IDs — used by the client pixel (layout / advanced matching)
// and by the server-side Conversion API (CAPI). Keep this list in sync with the
// inline init script in app/layout.tsx.
export const META_PIXEL_IDS = ['2203835897111572', '1499523888575243'] as const

export const META_GRAPH_VERSION = 'v21.0'
